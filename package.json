{
  "private": true,
  "scripts": {
    "data": "node preprocess-data.js",
    "build": "npm run data && mkdir -p dist && node build.js",
    "start": "http-server --port 9001 ."
  },
  "devDependencies": {
    "html-minifier": "^4.0.0",
    "http-server": "^0.12.3",
    "node-fetch": "^2.6.0",
    "puppeteer": "^3.0.4",
    "whatwg-fetch": "^3.0.0"
  }
}
